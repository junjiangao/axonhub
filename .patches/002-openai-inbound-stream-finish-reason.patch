diff --git a/llm/transformer/openai/inbound.go b/llm/transformer/openai/inbound.go
index 6112fddf..da2d5e7b 100644
--- a/llm/transformer/openai/inbound.go
+++ b/llm/transformer/openai/inbound.go
@@ -13,6 +13,7 @@ import (
 	"github.com/looplj/axonhub/llm/internal/pkg/xjson"
 	"github.com/looplj/axonhub/llm/streams"
 	transformer "github.com/looplj/axonhub/llm/transformer"
+	"github.com/samber/lo"
 )
 
 // InboundTransformer implements transformer.Inbound for OpenAI format.
@@ -104,9 +105,11 @@ func (t *InboundTransformer) TransformStream(
 	ctx context.Context,
 	stream streams.Stream[*llm.Response],
 ) (streams.Stream[*httpclient.StreamEvent], error) {
-	return streams.NoNil(streams.MapErr(stream, func(chunk *llm.Response) (*httpclient.StreamEvent, error) {
-		return t.TransformStreamChunk(ctx, chunk)
-	})), nil
+	return streams.NoNil(&openAIInboundStream{
+		source: stream,
+		ctx:    ctx,
+		t:      t,
+	}), nil
 }
 
 func (t *InboundTransformer) TransformStreamChunk(
@@ -130,6 +133,9 @@ func (t *InboundTransformer) TransformStreamChunk(
 		return nil, nil
 	}
 
+	// finish_reason synthesis is handled at the stream level by openAIInboundStream.
+	// See ensureStreamFinishReason.
+
 	// Convert to OpenAI Response format
 	oaiResp := ResponseFromLLM(chatResp)
 
@@ -145,6 +151,92 @@ func (t *InboundTransformer) TransformStreamChunk(
 	}, nil
 }
 
+// openAIInboundStream is a stateful stream that synthesizes finish_reason
+// for upstream providers that omit it (e.g. glm-5.1).
+//
+//nolint:containedctx // Checked.
+type openAIInboundStream struct {
+	source streams.Stream[*llm.Response]
+	ctx    context.Context
+	t      *InboundTransformer
+
+	// hasSeenToolCalls tracks whether any prior chunk contained tool calls,
+	// used to infer the correct finish_reason for usage-only terminal chunks.
+	hasSeenToolCalls    bool
+	hasSeenFinishReason bool
+
+	current *httpclient.StreamEvent
+	err     error
+}
+
+func (s *openAIInboundStream) Next() bool {
+	for s.source.Next() {
+		chunk := s.source.Current()
+		// Synthesize finish_reason before transforming the chunk.
+		ensureStreamFinishReason(chunk, &s.hasSeenToolCalls, &s.hasSeenFinishReason)
+		event, err := s.t.TransformStreamChunk(s.ctx, chunk)
+		if err != nil {
+			s.err = err
+			return false
+		}
+		if event == nil {
+			continue
+		}
+		s.current = event
+		return true
+	}
+	return false
+}
+
+func (s *openAIInboundStream) Current() *httpclient.StreamEvent {
+	return s.current
+}
+
+func (s *openAIInboundStream) Err() error {
+	if s.err != nil {
+		return s.err
+	}
+	return s.source.Err()
+}
+
+func (s *openAIInboundStream) Close() error {
+	return s.source.Close()
+}
+
+// ensureStreamFinishReason synthesizes finish_reason only for a terminal
+// usage-only chunk. Regular content and tool-call chunks remain non-terminal.
+// hasSeenToolCalls determines whether the terminal reason is "tool_calls" or
+// "stop" when the upstream provider omits finish_reason.
+func ensureStreamFinishReason(resp *llm.Response, hasSeenToolCalls, hasSeenFinishReason *bool) {
+	if resp == nil {
+		return
+	}
+
+	for i := range resp.Choices {
+		c := &resp.Choices[i]
+		if c.FinishReason != nil && *c.FinishReason != "" {
+			*hasSeenFinishReason = true
+		}
+		if c.Delta == nil || len(c.Delta.ToolCalls) == 0 {
+			continue
+		}
+		*hasSeenToolCalls = true
+	}
+
+	if !*hasSeenFinishReason && len(resp.Choices) == 0 && resp.Usage != nil {
+		reason := "stop"
+		if *hasSeenToolCalls {
+			reason = "tool_calls"
+		}
+		resp.Choices = append(resp.Choices, llm.Choice{
+			Index:        0,
+			Delta:        &llm.Message{},
+			FinishReason: lo.ToPtr(reason),
+		})
+		*hasSeenFinishReason = true
+	}
+}
+
 // isReasoningSignatureEvent checks if the response contains ONLY ReasoningSignature.
 // This is a helper function to filter out reasoning signature events when transforming
 // to OpenAI format, since OpenAI format doesn't support ReasoningSignature in streaming.
diff --git a/llm/transformer/openai/inbound_test.go b/llm/transformer/openai/inbound_test.go
index ce4ac7fd..a24b2b89 100644
--- a/llm/transformer/openai/inbound_test.go
+++ b/llm/transformer/openai/inbound_test.go
@@ -469,6 +469,7 @@ func TestInboundTransformer_TransformStreamChunk(t *testing.T) {
 					*chatResp.Choices[0].FinishReason == "tool_calls"
 			},
 		},
+
 		{
 			name: "empty choices",
 			response: &llm.Response{
@@ -483,6 +484,7 @@ func TestInboundTransformer_TransformStreamChunk(t *testing.T) {
 				return event.Type == ""
 			},
 		},
+
 		{
 			name:        "nil response",
 			response:    nil,
@@ -529,6 +531,140 @@ func TestInboundTransformer_TransformStreamChunk(t *testing.T) {
 	}
 }
 
+func TestInboundTransformer_TransformStream_SynthesizesFinishReason(t *testing.T) {
+	transformer := NewInboundTransformer()
+
+	t.Run("tool call chunk stays non-terminal and usage-only chunk gets tool_calls", func(t *testing.T) {
+		stream, err := transformer.TransformStream(t.Context(), streams.SliceStream([]*llm.Response{
+			{
+				ID:      "chatcmpl-123",
+				Object:  "chat.completion.chunk",
+				Created: 1677652288,
+				Model:   "gpt-4",
+				Choices: []llm.Choice{
+					{
+						Index: 0,
+						Delta: &llm.Message{
+							Role: "assistant",
+							ToolCalls: []llm.ToolCall{
+								{
+									ID:   "call_123",
+									Type: "function",
+									Function: llm.FunctionCall{
+										Name: "get_user_city",
+									},
+									Index: 0,
+								},
+							},
+						},
+					},
+				},
+			},
+			{
+				ID:      "chatcmpl-123",
+				Object:  "chat.completion.chunk",
+				Created: 1677652289,
+				Model:   "gpt-4",
+				Choices: []llm.Choice{},
+				Usage: &llm.Usage{
+					PromptTokens:     10,
+					CompletionTokens: 20,
+					TotalTokens:      30,
+				},
+			},
+			{
+				Object: "[DONE]",
+			},
+		}))
+		require.NoError(t, err)
+
+		var events []*httpclient.StreamEvent
+		for stream.Next() {
+			events = append(events, stream.Current())
+		}
+		require.NoError(t, stream.Err())
+		require.Len(t, events, 3) // tool call chunk + usage chunk + [DONE]
+
+		var toolCallChunk llm.Response
+		require.NoError(t, json.Unmarshal(events[0].Data, &toolCallChunk))
+		require.Nil(t, toolCallChunk.Choices[0].FinishReason)
+
+		// Usage-only chunk gets the terminal reason after all tool-call data.
+		var usageChunk llm.Response
+		require.NoError(t, json.Unmarshal(events[1].Data, &usageChunk))
+		require.Len(t, usageChunk.Choices, 1)
+		require.NotNil(t, usageChunk.Choices[0].FinishReason)
+		require.Equal(t, "tool_calls", *usageChunk.Choices[0].FinishReason)
+	})
+
+	t.Run("usage-only chunk without prior tool calls gets stop as finish_reason", func(t *testing.T) {
+		stream, err := transformer.TransformStream(t.Context(), streams.SliceStream([]*llm.Response{
+			{
+				ID:      "chatcmpl-123",
+				Object:  "chat.completion.chunk",
+				Created: 1677652288,
+				Model:   "gpt-4",
+				Choices: []llm.Choice{
+					{
+						Index: 0,
+						Delta: &llm.Message{
+							Role: "assistant",
+							Content: llm.MessageContent{
+								Content: lo.ToPtr("Hello"),
+							},
+						},
+					},
+				},
+			},
+			{
+				ID:      "chatcmpl-123",
+				Object:  "chat.completion.chunk",
+				Created: 1677652289,
+				Model:   "gpt-4",
+				Choices: []llm.Choice{},
+				Usage: &llm.Usage{
+					PromptTokens:     10,
+					CompletionTokens: 20,
+					TotalTokens:      30,
+				},
+			},
+			{
+				Object: "[DONE]",
+			},
+		}))
+		require.NoError(t, err)
+
+		var events []*httpclient.StreamEvent
+		for stream.Next() {
+			events = append(events, stream.Current())
+		}
+		require.NoError(t, stream.Err())
+		require.Len(t, events, 3) // text chunk + usage chunk + [DONE]
+
+		// Usage-only chunk (events[1]) without prior tool calls should get "stop"
+		var usageChunk llm.Response
+		require.NoError(t, json.Unmarshal(events[1].Data, &usageChunk))
+		require.Len(t, usageChunk.Choices, 1)
+		require.NotNil(t, usageChunk.Choices[0].FinishReason)
+		require.Equal(t, "stop", *usageChunk.Choices[0].FinishReason)
+	})
+
+	t.Run("usage-only chunk stays unchanged after upstream finish_reason", func(t *testing.T) {
+		hasSeenToolCalls := false
+		hasSeenFinishReason := false
+		ensureStreamFinishReason(&llm.Response{
+			Choices: []llm.Choice{{FinishReason: lo.ToPtr("stop")}},
+		}, &hasSeenToolCalls, &hasSeenFinishReason)
+
+		usageChunk := &llm.Response{
+			Choices: []llm.Choice{},
+			Usage:   &llm.Usage{TotalTokens: 30},
+		}
+		ensureStreamFinishReason(usageChunk, &hasSeenToolCalls, &hasSeenFinishReason)
+
+		require.Empty(t, usageChunk.Choices)
+	})
+}
 func TestInboundTransformer_TransformStream_SkipsPureReasoningSignatureChunk(t *testing.T) {
 	transformer := NewInboundTransformer()
 	signature := "stream_signature"
