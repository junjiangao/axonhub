diff --git a/Dockerfile b/Dockerfile
index 4cebbcc..a046490 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -18,20 +18,22 @@ FROM golang:alpine AS backend-builder
 
 WORKDIR /build
 
-RUN apk add --no-cache git ca-certificates tzdata
+RUN sed -i 's/dl-cdn.alpinelinux.org/mirrors.tuna.tsinghua.edu.cn/g' /etc/apk/repositories && \
+    apk add --no-cache git ca-certificates tzdata
 
 COPY go.mod go.sum ./
 COPY llm/go.mod llm/go.sum llm/
 RUN --mount=type=cache,target=/go/pkg/mod \
     --mount=type=cache,target=/root/.cache/go-build \
-    GOTOOLCHAIN=auto go mod download
+    GOTOOLCHAIN=auto GOPROXY=https://goproxy.cn,direct go mod download
 
 COPY . .
 COPY --from=frontend-dist /dist /build/internal/server/static/dist
 
 ENV GO111MODULE=on \
     CGO_ENABLED=0 \
-    GOOS=linux
+    GOOS=linux \
+    GOPROXY=https://goproxy.cn,direct
 
 RUN --mount=type=cache,target=/go/pkg/mod \
     --mount=type=cache,target=/root/.cache/go-build \
@@ -43,10 +45,11 @@ RUN --mount=type=cache,target=/go/pkg/mod \
 
 FROM alpine
 
-RUN apk add --no-cache ca-certificates tzdata
+RUN sed -i 's/dl-cdn.alpinelinux.org/mirrors.tuna.tsinghua.edu.cn/g' /etc/apk/repositories && \
+    apk add --no-cache ca-certificates tzdata
 
 WORKDIR /app
 COPY --from=backend-builder /build/axonhub /app/axonhub
 
 EXPOSE 8090
-ENTRYPOINT ["/app/axonhub"]
\ No newline at end of file
+ENTRYPOINT ["/app/axonhub"]
diff --git a/docker-compose.yml b/docker-compose.yml
index 71c2d235..e542e6c6 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -16,10 +16,17 @@ services:
   postgres:
     image: postgres:16-alpine
     container_name: axonhub-postgres
+    command:
+      - postgres
+      - -c
+      - timezone=Asia/Shanghai
+      - -c
+      - log_timezone=Asia/Shanghai
     environment:
       POSTGRES_DB: axonhub
       POSTGRES_USER: axonhub
       POSTGRES_PASSWORD: ${DB_PASSWORD:-axonhub_password}
+      TZ: Asia/Shanghai
     volumes:
       - postgres_data:/var/lib/postgresql/data
     ports:
@@ -35,12 +42,17 @@ services:
 
   # AxonHub 主服务
   axonhub:
-    image: looplj/axonhub:latest
+    build:
+      context: .
+      dockerfile: Dockerfile
+      network: host
+    image: axonhub:local
     container_name: axonhub-app
     environment:
       # 数据库配置可以通过环境变量设置，优先级高于配置文件
       AXONHUB_DB_DIALECT: postgres
       AXONHUB_DB_DSN: postgres://axonhub:${DB_PASSWORD:-axonhub_password}@postgres:5432/axonhub?sslmode=disable
+      TZ: Asia/Shanghai
     ports:
       - "8090:8090"
     volumes:
diff --git a/internal/build/VERSION b/internal/build/VERSION
index 6779af67..d9a99de0 100644
--- a/internal/build/VERSION
+++ b/internal/build/VERSION
@@ -1 +1 @@
-v0.9.38
+v0.9.43
diff --git a/.gitignore b/.gitignore
index 90c11378..2eaeef72 100755
--- a/.gitignore
+++ b/.gitignore
@@ -167,3 +167,4 @@ config.schema.json
 .omc/
 .playwright-mcp/
 .ace-tool/
+*.rej
